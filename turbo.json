{
    "$schema": "https://turbo.build/schema.json",
    "ui": "stream",
    "globalEnv": [],
    "tasks": {
        "build": {
            "dependsOn": ["^build"]
        },
        "clean": {
            "dependsOn": ["^clean"]
        }
    }
}
